angular.module('homemade')
  .config(function($stateProvider, $urlRouterProvider, $ionicConfigProvider) {
    $stateProvider.state('app.{{camelCase name}}', {
      url: '/{{camelCase name}}',
      views: {
        'menuContent': {
          templateUrl: 'app/{{dashCase name}}/{{dashCase name}}.html',
          controller: '{{properCase name}}Ctrl'
        }
      }
    })
  })
  .controller('{{properCase name}}Ctrl', function ($scope, $stateParams, Resource, image, $timeout, ionicMaterialInk, ionicMaterialMotion, initIonicView) {
    initIonicView($scope, ionicMaterialInk, ionicMaterialMotion);

    // Your code here
  });
