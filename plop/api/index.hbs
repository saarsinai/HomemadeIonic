'use strict';

import restful from 'node-restful';
import {{camelCase name}}Model from './{{name}}.model';

const mongoose = restful.mongoose;

export default app => {
  const {{properCase name}} = restful.model('{{name}}', {{camelCase name}}Model.schema)
    .methods(['get', 'post', 'put', 'delete']);

  {{properCase name}}.register(app, '/api/{{name}}');
};
