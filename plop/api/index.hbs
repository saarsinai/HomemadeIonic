'use strict';

import restful from 'node-restful';
import {{name}} from './{{name}}.model';

const mongoose = restful.mongoose;

export default app => {
  restful.model('{{name}}', {{name}}.schema)
    .methods(['get', 'post', 'put', 'delete'])
    .register(app, '/api/{{name}}');
};
